<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MiniProject-1</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 p-10">
        <div class="flex justify-end">
            <a class=" text-sm bg-red-600 rounded-md px-3 py-2 mb-5 inline-block" href="/logout">Logout</a>
        </div>
       <h3 class="text-white">Hello,<%= user.name %>👋</h3>
       <h5 class="text-white pb-5">Create a new Post</h5>

       <form action="/post" method="post">
        <textarea class=" w-1/3 resize-none bg-transparent block rounded-md outline-none text-white p-3 border-2 border-zinc-800 mb-3" placeholder="What is in your mind" name="content"></textarea>
        <input class="bg-blue-400 px-3 py-3 w-40 rounded-md block " type="submit" value="Creat Post">
       </form>

       <div class="posts mt-20">
        <h3 class="text-zinc-400 pb-2">Your Posts</h3>
        <% user.posts.forEach((post)=>{ %>
            <div class=" postcontainer mb-3 w-1/3 border-[1px] border-zinc-500 bg-zinc-800 text-white rounded-md p-3 mt-5">
                <h3 class="text-blue-400 mb-2">@<%= user.name %></h3>
                <div class="pb-2">
                    <p class="text-sm tracking-tight"><%= post.content %></p>
                </div>
                <small class="inline-block mt-1"><%= post.likes.length %> likes</small>
                <div class="btns flex gap-3 mt-2">
                    <a  class="text-blue-400 text-sm" href="/like/<%= post._id %>">
                        <%= post.likes.indexOf(user._id)===-1?"Like" : "Unlike"   %></a>
                    <a  class="text-zinc-600 text-sm" href="/edit/<%= post._id %>">edit</a>
                </div>
            </div>    
       <% }) %>

       </div>

    </div>
</body>
</html>